# Static external-access token for catalog API calls (Helm-injected appConfig).
global:
  lightspeed:
    enabled: false

upstream:
  backstage:
    appConfig:
      backend:
        auth:
          externalAccess:
            - type: static
              options:
                token: test-token
                subject: gitlab-events-e2e
