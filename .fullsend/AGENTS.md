# Fullsend instructions

When you encounter E2E test failures, use the `e2e-failure-analysis` and
`playwright-trace` skills to diagnose the root cause before attempting fixes.

If Claude auto-backgrounds a verification command, invoke `TaskStop` before
finishing.
